"""In-tree project registrations.

These manifests register the OMOP/FHIR components that still live inside the
core package, so the plugin mechanism is exercised end-to-end before the code
is extracted into a separate ``pluginlake-ehds-demo`` package (ADR-009,
Phase 3 of the split plan). They are temporary scaffolding.
"""
