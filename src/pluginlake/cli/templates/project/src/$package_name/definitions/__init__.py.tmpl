"""Dagster code location for the $project_id project.

Loaded by core's merged code-location loader via the project manifest. Add your
assets to ``defs`` below.
"""

from dagster import Definitions

defs = Definitions()
